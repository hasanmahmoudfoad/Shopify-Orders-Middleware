{
  "name": "shopify-order-monitor",
  "version": "0.1.0",
  "description": "Lightweight Shopify ORDERS_CREATE webhook monitor (in-memory)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cloudflared": "^0.7.1",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  }
}
